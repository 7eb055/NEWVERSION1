build:
  docker:
    web: backend/Dockerfile
setup:
  addons:
    - plan: heroku-postgresql:essential-0
  config:
    NODE_ENV: production
release:
  image: web
  command:
    - node
    - backend/initDb.js
